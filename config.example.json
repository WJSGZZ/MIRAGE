{
  "listen": "0.0.0.0:443",
  "serverKey": "REPLACE — run: ./miraged -genkey",
  "certFile": "",
  "keyFile":  "",
  "_note_cert": "Leave certFile/keyFile empty to auto-generate a self-signed cert (saved as mirage-cert.pem / mirage-key.pem). For production, provide real Let's Encrypt paths.",
  "maxTimeDiff": 60,
  "users": [
    {
      "name":    "user1",
      "shortId": "REPLACE — 2–16 hex chars, e.g. aabbccddeeff0011"
    }
  ]
}
